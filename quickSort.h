#include <stdio.h>
#include "node.h"
#include "graph.h"

class quickSort
{
    void qs(Node **lista, int limite_izq, int limite_der);
public:
    quickSort(/* args */);
    ~quickSort();
    void solve(Node **lista,int n);
    
};

